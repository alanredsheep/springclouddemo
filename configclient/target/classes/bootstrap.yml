spring:
  cloud:
    config:
      name: configclient
      label: master
      discovery:
        enabled: true
        # 也可以使用uri来查找
      uri: http://localhost:8888
#        service-id: configserver