server:
  port: 8762

spring:
  application:
    name: nativeconfigserver
  # 配置文件的获取方式
  profiles:
    active: native
  # 本地配置的存放路径
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared